import { EmptySetNullDirective } from './empty-set-null.directive';

describe('EmptySetNullDirective', () => {
  it('should create an instance', () => {
    const directive = new EmptySetNullDirective();
    expect(directive).toBeTruthy();
  });
});
