import { FirstLetterUpperDirective } from './first-letter-upper.directive';

describe('FirstLetterUpperDirective', () => {
  it('should create an instance', () => {
    const directive = new FirstLetterUpperDirective();
    expect(directive).toBeTruthy();
  });
});
